# Chép thành .env rồi điền — hai giá trị này là khoá CÔNG KHAI (web cũng lộ ra trình duyệt),
# chỉ đọc được tin ĐÃ DUYỆT. Tuyệt đối không đặt khoá service_role ở đây.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
